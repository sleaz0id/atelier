<h1>Books</h1>
<table>
  <thead>
    <tr>
      <th>title</th>
      <th>ISBN</th>
      <th>action</th>
    </tr>
  </thead>
  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.title %></td>
        <td><%= book.isbn %></td>
        <td><%= link_to('show', book_path(book.id)) %>
      </tr>
    <% end %>
  </tbody>
</table>
<%= link_to('add book', new_book_path) %>
